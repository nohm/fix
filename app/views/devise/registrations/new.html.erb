<% provide(:title, 'Badger - Sign up') %>

<%= bootstrap_form_for(resource, :as => resource_name, :url => registration_path(resource_name), style: :horizontal, left: "col-sm-2", right: "col-sm-6") do |f| %>
  <%= devise_error_messages! %>

  <legend>Sign up</legend>

  <%= f.text_field :name %>
  <%= f.email_field :email %>
  <%= f.password_field :password %>
  <%= f.password_field :password_confirmation %>
  
  <%= f.hidden_field :initial_role, :value => params[:initial_role] %>

  <%= f.form_group do %>
 	<%= f.submit 'Sign up', data: { disable_with: 'Signing up...' } %>
  <% end %>
<% end %>

<%= render "devise/shared/links" %>