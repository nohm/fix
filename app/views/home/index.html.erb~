<% if current_user.nil? %>
  <p>
    Welcome.
    Please login or sign up.
  </p>
<% else %>
  <h3>Entries</h3>
  <div class="span8">
    <table class="table table-condensed">
      <thead>
      <tr>
        <th>Number</th>
        <th>Brand</th>
        <th>Type</th>
        <th>Serial</th>
        <% if current_user.has_role? :manager or current_user.has_role? :admin %>
          <th></th>
          <th></th>
        <% end %>
      </tr>
    </thead>
    <tbody>
    <% @entries.each do |entry| %>
      <tr>
        <td><%= link_to entry.number, entry %></td>
        <td><%= entry.brand %></td>
        <td><%= entry.typenum %></td>
        <td><%= entry.serialnum %></td>
        <% if current_user.has_role? :manager or current_user.has_role? :admin %>
          <td><%= link_to("Edit entry", edit_entry_path(entry), :class => 'btn btn-mini')  %></td>
          <td><%= link_to("Delete entry", entry_path(entry), :data => { :confirm => "Are you sure?" }, :method => :delete, :class => 'btn btn-mini')  %></td>
        <% end %>
      </tr>
    <% end %>
    </tbody>
  </table>
  </div>
  <div class="span8"><%= link_to("Add entry", new_entry_path, :class => 'btn btn-mini')  %></div>
<% end %>
